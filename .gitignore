# ===== Data (symlink to shared storage / huge) =====
/data
/data/

# ===== workspace/ : code + artifacts are interleaved here =====
# Strategy: ignore EVERYTHING under workspace/, then whitelist only source/config files.
# This keeps experiment code while dropping checkpoints, datasets, big JSONs, downloads, etc.
workspace/**
# allow git to descend into subdirs
!workspace/**/
# whitelist source / config / docs only
!workspace/**/*.py
!workspace/**/*.sh
!workspace/**/*.yaml
!workspace/**/*.yml
!workspace/**/*.toml
!workspace/**/*.cfg
!workspace/**/*.ini
!workspace/**/*.md
!workspace/**/*.ipynb
# fold assignments are shared across all experiments — keep in git
!workspace/fold/**/*.csv
# expS01 (Astra 進捗共有): requirements / 調査 evidence / ガードレール .gitignore も共有
!workspace/expS01_atlas_synth/**/*.txt
!workspace/expS01_atlas_synth/assets/evidence/*.json
!workspace/expS01_atlas_synth/.gitignore
# Astra へ渡す較正資料。実データの集計値のみで、フレーム単位の派生物は含めない
!workspace/expS01_atlas_synth/assets_local/*.json

# ===== Generic experiment artifacts (top-level too) =====
wandb/
runs/
outputs/
output/
results/
preds/
predictions/
checkpoints/
ckpt/
ckpts/
logs/
log/
tb_logs/
tensorboard/
cache/
.cache/
tmp/
*.log

# ===== Python env =====
.venv/
.venv-render/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ===== Model weights (ship via scp, not git) =====
*.pth
*.pt
*.ckpt
*.safetensors
*.onnx
*.engine
*.bin
*.h5

# ===== Vendored external repos / starter kits / large reference assets =====
# (re-fetch from upstream or scp from the source machine)
/reference/
/reference_pe/

# ===== Submission packages: keep our own scripts, drop vendored repos & artifacts =====
submit/**/.git/
submit/**/weights/
# ローカル回帰テストの入出力（チャレンジデータそのもの。絶対に commit しない）
submit/**/test/input/
submit/**/test/output/
submit/**/hosttest/
submit/**/*.tar
submit/**/*.tar.gz
submit/**/*.zip

# ===== Media / large binaries =====
*.mp4
*.avi
*.mov
*.gif
*.ply
*.pkl
*.npy
*.npz

# ===== OS / editor =====
.DS_Store
*.swp
